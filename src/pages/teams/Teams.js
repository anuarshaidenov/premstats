import HeroSection from '../../components/hero-section/HeroSection';
import './teams.scss';

const Teams = () => {
  return (
    <div className="teams">
      <HeroSection />
    </div>
  );
};

export default Teams;
